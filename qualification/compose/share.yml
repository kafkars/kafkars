services:
  kafka-1:
    environment: &share-environment
      KAFKA_GROUP_COORDINATOR_REBALANCE_PROTOCOLS: classic,consumer,share
      KAFKA_GROUP_SHARE_MIN_RECORD_LOCK_DURATION_MS: 1000
      KAFKA_GROUP_SHARE_RECORD_LOCK_DURATION_MS: 2000

  kafka-2:
    environment: *share-environment

  kafka-3:
    environment: *share-environment
